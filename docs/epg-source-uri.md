# 节目单链接

支持以下格式：`xml`、`xml.gz`、`diyp`、`lovetv`，默认缓存时间为1天。

```
http://example.com/epg.xml
http://example.com/epg.xml.gz

http://dipy.example.com/index.php?ch={name}&date={date}
http://lovetv.example.com/index.php?channel={name}&date={date}
```
